node_modules/
*.log

# Uploads folder - ignore content but keep structure
uploads/*
!uploads/.gitkeep

# Temp folder - ignore content but keep structure
temp/*
!temp/.gitkeep

# Archivos de entorno con credenciales (Wasabi, DB, JWT, etc.)
# Solo se versiona .env.example como template.
.env
.env.local
.env.*.local
